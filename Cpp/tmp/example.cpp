#include <iostream>
#include <unordered_map>
#include <string>
std::map<string, string> Umap_options =
{
  {"alpha", "tcr_alpha" },
  {"beta", "tcr_beta" },
  {"alpha", "tcr_alpha" },
}

int main(){
  std::cout << "hodfas " << std::endl;
  return 0;
}
//string defaultPath = string(IGOR_DATA_DIR) + "/models/"+species_str+"/"+chain_path_str+"/ref_genome/genomicVs.fasta"
